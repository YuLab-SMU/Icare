% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_06_ensemble.R
\name{PlotModelComparison}
\alias{PlotModelComparison}
\title{Multi-Metric Faceted Bar Chart}
\usage{
PlotModelComparison(
  model_obj,
  top_n = 4,
  metrics = c("auc", "Sensitivity", "Specificity", "accuracy_score", "f1_score"),
  palette_name = "Darjeeling1",
  base_size = 13,
  save_plot = FALSE,
  save_dir = NULL,
  width = 10,
  height = 6
)
}
\arguments{
\item{model_obj}{A trained Train_Model object.}

\item{top_n}{Number of top models. Default 4.}

\item{metrics}{Metrics to display. Default: c("auc", "Sensitivity", 
"Specificity", "accuracy_score", "f1_score").}

\item{palette_name}{Wesanderson palette. Default "Darjeeling1".}

\item{base_size}{Base font size. Default 13.}

\item{save_plot}{Save? Default FALSE.}

\item{save_dir}{Output directory.}

\item{width, height}{Plot dimensions.}
}
\value{
A ggplot object.
}
\description{
Multi-Metric Faceted Bar Chart
}
\examples{
\dontrun{
mtcars$am <- as.factor(mtcars$am)
model <- CreateModelObject(data = mtcars, group_col = "am")
set.seed(123)
idx <- sample(1:nrow(mtcars), 20)
model@filtered.set <- list(training = mtcars[idx, ], testing = mtcars[-idx, ])
trained <- ModelTrainAnalysis(model, methods = c("glm", "rf"),
                              control = list(method = "cv", number = 3),
                              save_plots = FALSE)
if (interactive()) {
  PlotModelComparison(trained, top_n = 2, save_plot = FALSE)
  PlotModelHeatmap(trained, save_plot = FALSE)
  PlotModelParallel(trained, top_n = 2, save_plot = FALSE)
}
}
}
