% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subtyping_03_kmeans.R
\name{Sub_kmeans_with_optimal_k}
\alias{Sub_kmeans_with_optimal_k}
\title{Sub K-means with Optimal K}
\usage{
Sub_kmeans_with_optimal_k(
  object,
  use_scaled_data = TRUE,
  palette_name = "Zissou1",
  save_plots = TRUE,
  save_dir = file.path(get_output_dir("Subtyping", "cluster_results"), "kmeans_result"),
  plot_width = 6,
  plot_height = 5,
  base_size = 14,
  nstart = 50,
  k.max = 15,
  seed = 123
)
}
\arguments{
\item{object}{Subtyping object (or numeric data frame).}

\item{use_scaled_data}{Use the \code{scale.data} slot when \code{TRUE}.}

\item{palette_name}{wesanderson palette name for cluster colours.}

\item{save_plots}{Whether to save plots.}

\item{save_dir}{Output directory.}

\item{plot_width}{Plot width (inches).}

\item{plot_height}{Plot height (inches).}

\item{base_size}{Base font size.}

\item{nstart}{Number of K-means random starts.}

\item{k.max}{Maximum K to search.}

\item{seed}{Random seed.}
}
\description{
Wraps \code{kmeans_with_optimal_k} for use with a \code{Subtyping} object.
}
\examples{
\dontrun{
set.seed(1)
demo_df <- data.frame(
  id    = paste0("S", 1:60),
  group = rep(c(0, 1), each = 30),
  feat1 = c(rnorm(30, 5, 1), rnorm(30, 8, 1)),
  feat2 = c(rnorm(30, 2, 0.5), rnorm(30, 4, 0.5)),
  feat3 = rnorm(60, 10, 2),
  feat4 = c(rnorm(30, 1, 0.3), rnorm(30, 3, 0.3))
)
stat_obj <- CreateStatObject(raw.data = demo_df, clean.data = demo_df,
                             group_col = "group", na.action = "allow")
sub_obj <- ConvertObject(stat_obj, to = "Subtyping")
sub_obj <- Sub_normalize_process(sub_obj, normalize_method = "min_max")

sub_obj <- Sub_kmeans_with_optimal_k(
  sub_obj, use_scaled_data = TRUE, k.max = 4,
  save_plots = FALSE, seed = 1
)
table(sub_obj@info.data$cluster_kmeans)
}
}
